# 原始下載檔一律不進 git：96MB 健保 CSV 與 79MB TFDA JSON 每月可重抓，
# 進 history 一年就是 1GB 以上，且沒有任何 provenance 價值（快照才有）。
data/raw/
data/build/
node_modules/
dist/
.DS_Store
*.pyc
__pycache__/

# 離線包每月重產、單檔 15–19 MB，進 git 一年就是 400 MB。
# 只有 MANIFEST.txt 進版控，方便回溯「某月帶出去的是哪一版」。
dist-offline/
offline/*.html
offline/*.zip
offline/READ-ME-FIRST.txt

# ★ 臨床註記絕不進 git。這是 public repo —— 醫師寫的註記可能含病人資訊，
#   一旦 commit 就永遠留在公開歷史裡（刪掉也還在 reflog 與別人的 clone）。
#   註記只存在瀏覽器 localStorage，匯出的 .md 請留在本機。
curation/clinical_notes/
*clinical-notes*.md
